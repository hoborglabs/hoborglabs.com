##
# PRODUCTION configuration
#
# override anything that is not defined in main config.yml

imports:
  - { resource: ./../config.yml }
  - { resource: ./../parameters.ini }
  - { resource: ./parameters.ini }
